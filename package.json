{
  "name": "processors",
  "version": "1.0.0",
  "scripts": {
    "build": "./build.sh",
    "apply": "pnpm build && terraform apply -auto-approve",
    "plan": "pnpm build && terraform plan"
  },
  "main": "dist/index.js",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.564.0",
    "cheerio": "^1.0.0-rc.12",
    "nlcst-to-string": "^4.0.0",
    "retext": "^9.0.0",
    "retext-keywords": "^8.0.1",
    "retext-pos": "^5.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "esbuild": "^0.21.4"
  }
}