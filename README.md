# Feed Enhancer

This is a collection of Lambda functions that are used by the MongoDB app to enrich article data with summaries and likeness score.

## Mongo dump

For now this repository also hosts a Lambda function that dumps the mongo db cluster every day at midnight. But ideally this should be moved somewhere else.